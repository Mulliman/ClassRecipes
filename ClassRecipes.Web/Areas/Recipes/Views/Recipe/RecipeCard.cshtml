@model ClassRecipes.Core.IRelatedRecipe

@if (!string.IsNullOrEmpty(Model.ThinImage))
{
    @await Html.PartialAsync("RecipeCardWithImage", Model)
}
else
{
    @await Html.PartialAsync("RecipeCardNoImage", Model)
}