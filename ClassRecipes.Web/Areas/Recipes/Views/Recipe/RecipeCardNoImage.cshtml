@model ClassRecipes.Core.IRelatedRecipe

@if(Model == null)
{
    return;
}

<div class="card">
    <div class="card-content">
        <span class="card-title">@Model.Title</span>
        <p>
            @Model.ShortIntro
        </p>
    </div>

    

        @if (!string.IsNullOrEmpty(Model.UrlName))
        {
            <!--<div class="card-action">
                <a href="#">Basic chocolate frosting</a>
            </div>-->
        }

</div>